---
sidebar_position: 5
---

# 完全載入
- 等待擴充完全載入後，再繼續下一步動作(載入其他擴充)

:::danger
- 這個選項通常不需要使用。
- 如果不清楚此選項用意，不要輕易使用。
:::
:::tip
- 通常在使用了 `Inject 注入` 的擴充中，需要啟用此功能，確保 `Inject 注入` 完成。
:::

## 用法
- 在 `cdps.json` 中 聲明
```json
{
    "version": "1.0.0",
    "focus-load": true,     // 新增這行
    // ...
}
```